# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# node (only if you add tooling later)
node_modules/
npm-debug.log*

# local secrets — never commit real keys
.env
.env.local

# the original family book scans are private; keep them OUT of the public repo
*.pdf
Kong_Family_book*
